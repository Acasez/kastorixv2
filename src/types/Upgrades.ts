export type RunegunUpgrades = {
  name: string;
  description: string;
  prerequisites: string;
  level: number;
};

export type ArmorUpgrades = {
  name: string;
  description: string;
  prerequisites: string;
  level: number;
};
